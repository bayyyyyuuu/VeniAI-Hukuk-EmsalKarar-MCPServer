# ==============================================================================
# YARGITAY EMSAL KARAR MCP SERVER - ORTAM DEĞİŞKENLERİ ŞABLONU
# ==============================================================================
# Bu dosya, uygulamanın çalışması için gereken yapılandırma ayarlarnın bir örneğidir.
# Gerçek değerlerinizi kullanmak için '.env' adında bir dosya oluşturup bu değerleri kopyalayın.

# ------------------------------------------------------------------------------
# VERİTABANI AYARLARI (PostgreSQL)
# ------------------------------------------------------------------------------
# Analitikler ve L2 (Kalıcı) Önbellek için veritabanı bağlantısı gereklidir.
DATABASE_URL=postgresql://user:password@host:5432/database_name?sslmode=require
DATABASE_TYPE=postgresql
DATABASE_POOL_MIN=2
DATABASE_POOL_MAX=10

# ------------------------------------------------------------------------------
# BROWSERLESS (PUPPETEER) AYARLARI
# ------------------------------------------------------------------------------
# Yargıtay sitesinden veri çekmek için kullanılan tarayıcı otomasyon servisidir.
# Browserless bulut üzerinden (wss://) veya yerel docker üzerinden çalışabilir.
BROWSERLESS_URL=wss://chrome.browserless.io
BROWSERLESS_TOKEN=YOUR_SECRET_TOKEN
BROWSERLESS_TIMEOUT=30000
BROWSERLESS_RETRIES=3

# ------------------------------------------------------------------------------
# KAZIMA (SCRAPING) AYARLARI
# ------------------------------------------------------------------------------
# Tek bir sorguda kaç adet sonuç getirilmesini istediğinizi belirler.
SCRAPING_MAX_RESULTS=10
# Detay sayfalarından aynı anda kaç adet karar çekileceğini kontrol eder.
SCRAPING_PARALLEL_REQUESTS=3
SCRAPING_ADAPTIVE_TIMEOUT=true
SCRAPING_RETRY_STRATEGY=exponential
SCRAPING_BASE_TIMEOUT=10000

# ------------------------------------------------------------------------------
# ÖNBELLEK (CACHE) AYARLARI
# ------------------------------------------------------------------------------
# L1 (RAM) Önbellek süresi - Milisaniye cinsinden (Örn: 300000ms = 5 dk)
CACHE_L1_TTL_MS=300000
# L2 (DB) Önbellek geçerlilik süresi - Gün cinsinden
CACHE_L2_TTL_DAYS=30
# Bir sorgunun 'popüler' sayılması için gereken minimum arama sayısı.
CACHE_POPULAR_THRESHOLD=5
# Otomatik önbellek temizleme aralığı - Milisaniye (Örn: 3600000ms = 1 saat)
CACHE_CLEANUP_INTERVAL_MS=3600000

# ------------------------------------------------------------------------------
# UYGULAMA AYARLARI
# ------------------------------------------------------------------------------
# 'development' veya 'production' olarak ayarlayabilirsiniz.
NODE_ENV=development
